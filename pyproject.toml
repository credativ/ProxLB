[tool.mypy]
strict = true
python_version = "3.12"

[[tool.mypy.overrides]]
# proxlb-solver is an optional runtime dependency; ignore it when not installed.
module = ["proxlb_solver.*"]
ignore_missing_imports = true
